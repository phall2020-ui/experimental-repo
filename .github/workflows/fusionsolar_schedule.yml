name: FusionSolar Monitoring

on:
  schedule:
    # Inverter checks during UK daylight hours
    # Note: cron is in UTC. During BST (late Mar–late Oct) these run 1h later UK time.
    # Winter (GMT):  08:00, 10:30, 13:00, 15:30 UK
    # Summer (BST):  09:00, 11:30, 14:00, 16:30 UK  (still within daylight)
    - cron: '0 8 * * *'     # Check 1
    - cron: '30 10 * * *'   # Check 2
    - cron: '0 13 * * *'    # Check 3
    - cron: '30 15 * * *'   # Check 4
    # Daily data sync runs at 04:00 UTC to capture the previous day's complete data
    - cron: '0 4 * * *'     # Daily sync (previous day)
  workflow_dispatch:
    inputs:
      task:
        description: 'Task to run'
        required: true
        default: '--report'
        type: choice
        options:
          - '--check'
          - '--report'
          - '--report --no-sync'
          - '--test-login'
          - '--sync-yesterday'
          - '--backfill'
          - '--backfill --start-date 2025-12-01'

env:
  NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate secrets
        run: |
          missing=""
          if [ -z "${{ secrets.FUSIONSOLAR_USERNAME }}" ]; then missing="$missing FUSIONSOLAR_USERNAME"; fi
          if [ -z "${{ secrets.FUSIONSOLAR_PASSWORD }}" ]; then missing="$missing FUSIONSOLAR_PASSWORD"; fi
          if [ -z "${{ secrets.NOTION_TOKEN }}" ]; then missing="$missing NOTION_TOKEN"; fi
          if [ -n "$missing" ]; then
            echo "::error::Missing required secrets:$missing"
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Generate config.json
        run: |
          cat > config.json << 'TEMPLATE'
          {
            "station_code": "NE=199487890",
            "station_name": "Point Lane Solar Farm",
            "installed_capacity_kwp": 8601,
            "domain": "eu5.fusionsolar.huawei.com",
            "portal_base": "https://uni001eu5.fusionsolar.huawei.com/uniportal/pvmswebsite/assets/build/cloud.html",
            "portal_params": "app-id=smartpvms&instance-id=smartpvms&zone-id=region-1-43b6d703-7da9-49f0-8b84-048ff489273f",
            "credentials": {
              "username": "PLACEHOLDER_USER",
              "password": "PLACEHOLDER_PASS"
            },
            "notion_token": "PLACEHOLDER_TOKEN",
            "notion_parent_page_id": "2d21773a-96c0-8092-9070-d2128500f9dc",
            "stark": {
              "username": "PLACEHOLDER_STARK_USER",
              "password": "PLACEHOLDER_STARK_PASS",
              "site_name": "Point Lane",
              "search_text": "2100042103940"
            },
            "location": {
              "latitude": 51.5,
              "longitude": -0.1,
              "timezone": "Europe/London"
            },
            "schedule": {
              "check_times": ["08:00", "10:30", "13:00", "15:30"],
              "report_time": "22:00"
            },
            "logging": {
              "log_dir": "logs",
              "inverter_csv": "logs/inverter_checks.csv",
              "generation_csv": "logs/daily_generation.csv",
              "log_file": "logs/monitor.log"
            }
          }
          TEMPLATE

          # Inject secrets via jq
          jq --arg u "${{ secrets.FUSIONSOLAR_USERNAME }}" \
             --arg p "${{ secrets.FUSIONSOLAR_PASSWORD }}" \
             --arg t "${{ secrets.NOTION_TOKEN }}" \
             --arg su "${{ secrets.STARK_USERNAME }}" \
             --arg sp "${{ secrets.STARK_PASSWORD }}" \
             --arg ss "${{ secrets.STARK_SITE_NAME }}" \
             --arg sx "${{ secrets.STARK_SEARCH_TEXT }}" \
             '.credentials.username = $u
              | .credentials.password = $p
              | .notion_token = $t
              | .stark.username = ($su // "")
              | .stark.password = ($sp // "")
              | .stark.site_name = (if ($ss // "") == "" then "Point Lane" else $ss end)
              | .stark.search_text = (if ($sx // "") == "" then "2100042103940" else $sx end)' \
             config.json > config.tmp && mv config.tmp config.json

      - name: Create logs directory
        run: mkdir -p logs

      - name: Determine task
        id: task
        run: |
          YESTERDAY=$(date -u -d "yesterday" +%Y-%m-%d)
          echo "yesterday=$YESTERDAY" >> $GITHUB_OUTPUT
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "command=${{ github.event.inputs.task }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event.schedule }}" = "0 4 * * *" ]; then
            echo "command=--backfill --start-date $YESTERDAY --end-date $YESTERDAY" >> $GITHUB_OUTPUT
          else
            echo "command=--check" >> $GITHUB_OUTPUT
          fi
          echo "Task: $(cat $GITHUB_OUTPUT)"

      - name: Run FusionSolar Task
        id: run_task
        shell: bash
        run: |
          CMD="${{ steps.task.outputs.command }}"
          YESTERDAY="${{ steps.task.outputs.yesterday }}"
          EXIT_CODE=0
          if [[ "$CMD" == "--sync-yesterday" ]]; then
            echo "::group::Running FusionSolar sync for yesterday ($YESTERDAY)"
            echo "Command: python notion_sync.py --backfill --start-date $YESTERDAY --end-date $YESTERDAY"
            python notion_sync.py --backfill --start-date $YESTERDAY --end-date $YESTERDAY || EXIT_CODE=$?
            echo "::endgroup::"
          elif [[ "$CMD" == *"--backfill"* ]]; then
            echo "::group::Running Backfill"
            echo "Command: python notion_sync.py $CMD"
            python notion_sync.py $CMD || EXIT_CODE=$?
            echo "::endgroup::"
          elif [[ "$CMD" == *"--test-login"* ]]; then
            echo "::group::Testing Login"
            python fusionsolar_monitor.py --test-login || EXIT_CODE=$?
            echo "::endgroup::"
          else
            echo "::group::Running Monitor"
            echo "Command: python fusionsolar_monitor.py $CMD"
            python fusionsolar_monitor.py $CMD || EXIT_CODE=$?
            echo "::endgroup::"
          fi

          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          if [ $EXIT_CODE -ne 0 ]; then
            echo "::warning::Task exited with code $EXIT_CODE"
          fi

      - name: Run Stark Sync
        id: run_stark
        if: >
          github.event.schedule == '0 4 * * *' ||
          github.event.inputs.task == '--sync-yesterday'
        shell: bash
        run: |
          DATE="${{ steps.task.outputs.yesterday }}"
          if [ -z "$DATE" ]; then
            echo "::error::Yesterday date is empty -- cannot run Stark sync"
            exit 1
          fi
          EXIT_CODE=0
          echo "::group::Running Stark sync for $DATE"
          python stark_daily_sync.py --start $DATE --end $DATE || EXIT_CODE=$?
          echo "::endgroup::"
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          if [ $EXIT_CODE -ne 0 ]; then
            echo "::warning::Stark sync exited with code $EXIT_CODE"
          fi

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fusionsolar-logs-${{ github.run_id }}
          path: logs/
          retention-days: 30

      - name: Report status
        if: always()
        run: |
          EXIT_CODE="${{ steps.run_task.outputs.exit_code }}"
          STARK_EXIT="${{ steps.run_stark.outputs.exit_code }}"
          CMD="${{ steps.task.outputs.command }}"
          FAILED=0
          if [ "$EXIT_CODE" != "0" ] && [ -n "$EXIT_CODE" ]; then
            echo "::error::FusionSolar task '$CMD' failed (exit code: $EXIT_CODE). Check logs artifact for details."
            FAILED=1
          else
            echo "FusionSolar task '$CMD' completed successfully."
          fi
          if [ "$STARK_EXIT" != "0" ] && [ -n "$STARK_EXIT" ]; then
            echo "::error::Stark sync failed (exit code: $STARK_EXIT). Check logs artifact for details."
            FAILED=1
          elif [ "$STARK_EXIT" = "0" ]; then
            echo "Stark sync completed successfully."
          fi
          [ $FAILED -eq 0 ] || exit 1
