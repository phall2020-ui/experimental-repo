# experimental-repo — Actions

**Status:** Consolidation workspace. Acts as an umbrella for non-core projects and archived work.

## Suggested actions

- [ ] Audit `apps/` and `archive/` sub-directories — document what each contains so it is clear what is active vs archived
- [ ] Move any active projects that have grown into standalone repos out of this umbrella (they should have their own git repos at `/Users/peterhall/Projects/`)
- [ ] If this is purely an archive, consider adding a clear `ARCHIVE.md` listing what is here and why it was parked
