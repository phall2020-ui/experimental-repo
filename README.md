# experimental-repo

Consolidated experimental workspace for non-core projects.

## Structure

- `apps/`: active experimental repositories imported with history
- `archive/`: historical/support folders that are not active apps
